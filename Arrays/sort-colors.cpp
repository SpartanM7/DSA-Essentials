#include <iostream>
#include <vector>
using namespace std;
// method 1. using sort 
int sortmethod(vector<int>* arr, int n){


    return 0;
}


// method 2. couting the no. and printing them
int countingmethod(vector<int>* arr, int n){


    return 0;
}

// nu,ber 3.using repalce method
int replacemethod(vector<int> *arr, int n){

    return 0;
}

int main() {
    
    vector<int> arr(10);
    int n;
    cout << "Enter the amount of elements in the array:" << "\n";
    cin >> n;
    for( int i =0; i<=n; i++){
        arr.push_back();
    }
    
    sortmethod(arr, n);
    countingmethod(arr, n);
    replacemethod(arr, n);

    return 0;
}